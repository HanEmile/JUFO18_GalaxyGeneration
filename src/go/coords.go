// Generate Star coordinates using a custom function

// arg1 = number of stars
// arg2 = save path

package main

import (
  "fmt"
  "os"
)

func main(){

  arguments := os.Args[1:]

  fmt.Println(arguments)
}
